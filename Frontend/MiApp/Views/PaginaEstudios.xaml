<Page x:Class="ProyectoTurnos_FrontEnd.MiApp.Views.PaginaEstudios"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ProyectoTurnos_FrontEnd.MiApp.Views"
      mc:Ignorable="d"
      Title="PaginaEstudios" Height="450" Width="800">
    <Grid>
        <!--
        <Border Grid.Row="4" BorderBrush="#145A54" BorderThickness="1.5" CornerRadius="5" Margin="3">
            <WrapPanel VerticalAlignment="Center">
                <ListView x:Name="ImagenesHistoria" ItemsSource="{Binding imagenesHistoria}" Padding="10,0,0,0" HorizontalAlignment="Stretch" BorderThickness="0" Background="Transparent">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Button Cursor="Hand" Background="Transparent" BorderThickness="0">
                                <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                    <Image Source="{Binding imagen}" Width="70" Height="70" HorizontalAlignment="Stretch"/>
                                    <TextBlock Text="{Binding titulo}" FontWeight="Medium" Foreground="#145A54" HorizontalAlignment="Stretch" Margin="0,3,0,0"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                    
                <Border Width="70" Height="70" BorderBrush="#145A54" Background="Transparent" BorderThickness="1.5" CornerRadius="5" Margin="10,3,3,3">
                    <Button Click="AgregarImagen" Cursor="Hand" Background="Transparent" BorderThickness="0"/>
                </Border>
            </WrapPanel>
        </Border>
        -->
    </Grid>
</Page>
